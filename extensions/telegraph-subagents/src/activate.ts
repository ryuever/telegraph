export function activateTelegraphSubagentsExtension(): void {
  // The first implementation is declarative: manifest contributions are loaded
  // into the run snapshot before activation. This hook reserves the stable
  // extension entrypoint for future runtime-only bindings.
}
