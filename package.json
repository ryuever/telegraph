{
  "name": "telegraph-monorepo",
  "version": "1.0.0",
  "description": "Telegraph workspace root.",
  "private": true,
  "scripts": {
    "start": "pnpm --filter telegraph start",
    "package": "pnpm --filter telegraph package",
    "make": "pnpm --filter telegraph make",
    "dev": "pnpm --filter telegraph dev",
    "lint": "pnpm -r lint"
  }
}
