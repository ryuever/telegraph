{
  "name": "@telegraph/agent-protocol",
  "version": "0.0.0",
  "description": "Telegraph agent protocol — serializable AgentEvent, run, tool, extension, permission, and workflow types.",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
